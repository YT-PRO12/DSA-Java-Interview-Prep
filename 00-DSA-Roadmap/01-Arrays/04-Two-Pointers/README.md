04. Two Pointers
Overview

The Two Pointers technique is an important array and string problem-solving pattern.

Instead of using nested loops to compare every possible pair of elements, we maintain two indices and move them intelligently based on the problem conditions.

This can reduce many problems from O(n²) to O(n).
